---
interface Props {
  content?: string;
}

const {
  content = "Je suis animé par la conviction que l'analyse de données est un levier puissant pour répondre aux grands défis sociétaux et environnementaux de demain. Actuellement en pleine montée en compétences sur l'écosystème Data, je combine ma formation académique avec une veille technologique constante. Mais je ne suis pas qu'un profil technique : je crois fermement à l'importance de l'ouverture d'esprit. Mon profil s'enrichit d'un fort intérêt pour la culture générale, indispensable pour donner du sens aux chiffres, et d'un goût prononcé pour le sport qui canalise mon énergie. De nature curieuse, je suis également un linguophile amateur, appréciant la découverte de nouvelles cultures et la logique des mots, une gymnastique intellectuelle qui sert mon esprit d'analyse au quotidien.",
} = Astro.props;
---

<section id="about" class="about section-animate" aria-labelledby="about-heading">
  <div class="about-container">
    <h2 id="about-heading" class="section-title">À propos</h2>
    <p class="about-content">{content}</p>
  </div>
</section>

<style>
  .about {
    padding: var(--spacing-4xl) var(--spacing-xl);
    background-color: var(--color-pure-white);
  }

  .about-container {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .section-title {
    font-family: var(--font-family);
    font-size: var(--font-size-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-gray);
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-sm);
    border-bottom: 1px solid var(--color-gray-lighter);
  }

  .about-content {
    font-size: var(--font-size-lg);
    line-height: 1.85;
    color: var(--color-dark);
  }

  @media (max-width: 768px) {
    .about {
      padding: var(--spacing-3xl) var(--spacing-lg);
    }

    .about-content {
      font-size: var(--font-size-base);
    }
  }
</style>
