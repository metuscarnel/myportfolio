---
interface Props {
  content?: string;
}
---

<section id="about" class="about section-animate" aria-labelledby="about-heading">
  <div class="about-container">
    <h2 id="about-heading" class="section-title">À propos</h2>
    <p class="about-content">
       Actuellement en découverte de l'écosystème Data, je complète ma formation académique avec une veille technologique quasi quotidienne. Toutefois je ne suis pas qu'un profil technique : je suis ouvert d'esprit. J'éprouve un fort intérêt pour la <span class="highlight">culture générale</span>, et d'un goût prononcé pour le <span class="highlight">sport</span>. De nature curieuse, je suis également un amateur de geopolitique, de politique et de philosophie.
    </p>
  </div>
</section>

<style>
  .about {
    padding: var(--spacing-4xl) var(--spacing-xl);
    background-color: var(--bg-tertiary);
  }

  .about-container {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .section-title {
    font-family: var(--font-family);
    font-size: var(--font-size-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--text-secondary);
    margin-bottom: var(--spacing-md);
    padding-bottom: var(--spacing-sm);
    border-bottom: 1px solid var(--border-color);
  }

  .about-content {
    font-size: var(--font-size-lg);
    line-height: 1.85;
    color: var(--text-primary);
    padding: var(--spacing-sm);
    text-align: justify;
  }

  .highlight {
    font-weight: 700;
    color: var(--text-primary);
  }

  @media (max-width: 768px) {
    .about {
      padding: var(--spacing-3xl) var(--spacing-lg);
    }

    .about-content {
      font-size: var(--font-size-base);
      padding: var(--spacing-md);
    }
  }
</style>
